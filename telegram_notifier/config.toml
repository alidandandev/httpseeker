# Telegram Bot 配置
[telegram]
# Telegram Bot Token (从 @BotFather 获取)
bot_token = "6968270418:AAGlX7tNpfnIuueB2hAssZWzwuNCMjR6ryE"

# Telegram Chat ID (可以是个人ID或群组ID，从 https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates 获取)
chat_id = "-4781359846"

# 是否启用推送
send = true

# 代理配置 (如果需要使用代理，填写代理地址，否则留空)
[telegram.proxies]
http = ""
https = ""

# 报告配置
[report]
# 项目名称（与 HttpSeeker 项目配置中的 PROJECT_NAME 对应）
# 注意：如果在 Jenkins 环境中运行，会自动从环境变量 JOB_NAME 获取，无需手动配置
project_name = "Dz_like_bofa_h5"

# 测试人员名称（与 HttpSeeker 项目配置中的 TESTER_NAME 对应）
tester_name = "Rookie"

# Jenkins Allure 报告地址
# 注意：如果在 Jenkins 环境中运行，会自动从环境变量 BUILD_URL 生成，无需手动配置
# 格式: http://jenkins地址/job/项目名/allure/
jenkins_url = "http://47.76.165.189:8080/job/Dz_Like_bofa_h5/allure/"
